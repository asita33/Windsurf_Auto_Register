# 📧 自建邮件系统完整方案

## 🎯 当前状态

**已保留的服务**：
- ✅ **NiMail** - 唯一能接收 Windsurf 验证码的服务

**已删除的服务**：
- ❌ Maildrop
- ❌ TempMail
- ❌ Guerrilla Mail
- ❌ 10分钟邮箱

---

## 💡 自建邮件系统方案

### 方案 1: 使用 Docker + MailCatcher (免费) ⭐ 推荐

**特点**：
- ✅ 完全免费
- ✅ 本地运行
- ✅ 能接收所有邮件
- ✅ 有 Web 界面
- ✅ 提供 API

**技术栈**：
- Docker
- MailCatcher (Ruby)
- SMTP 服务器

**实现步骤**：

#### 1. 安装 Docker
```bash
# Windows: 下载 Docker Desktop
https://www.docker.com/products/docker-desktop/

# 或使用 WSL2
wsl --install
```

#### 2. 运行 MailCatcher
```bash
docker run -d -p 1080:1080 -p 1025:1025 schickling/mailcatcher
```

#### 3. 配置说明
```
SMTP 服务器: localhost:1025
Web 界面: http://localhost:1080
```

#### 4. 使用方法
```
1. 启动 MailCatcher
2. 任何发送到 localhost:1025 的邮件都会被捕获
3. 访问 http://localhost:1080 查看邮件
4. 通过 API 获取邮件内容
```

**API 示例**：
```javascript
// 获取所有邮件
GET http://localhost:1080/messages

// 获取邮件详情
GET http://localhost:1080/messages/:id.json

// 获取邮件 HTML
GET http://localhost:1080/messages/:id.html
```

**优点**：
- ✅ 完全免费
- ✅ 本地运行，隐私安全
- ✅ 能接收所有邮件
- ✅ 有完整的 API

**缺点**：
- ❌ 只能本地使用
- ❌ 不能接收外部邮件（需要配置域名和 DNS）

---

### 方案 2: 使用 Mailgun (付费，但有免费额度) ⭐ 推荐

**特点**：
- ✅ 每月 5000 封免费
- ✅ 官方 API
- ✅ 稳定可靠
- ✅ 能接收真实邮件

**价格**：
```
免费版: 5000 封/月
付费版: $35/月 起
```

**实现步骤**：

#### 1. 注册 Mailgun
```
https://www.mailgun.com/
```

#### 2. 验证域名
```
1. 添加你的域名
2. 配置 DNS 记录
3. 等待验证
```

#### 3. 获取 API Key
```
在控制台获取 API Key
```

#### 4. 使用 API
```javascript
const mailgun = require('mailgun-js')({
  apiKey: 'your-api-key',
  domain: 'your-domain.com'
});

// 发送邮件
mailgun.messages().send({
  from: 'test@your-domain.com',
  to: 'recipient@example.com',
  subject: 'Test',
  text: 'Hello'
});

// 接收邮件（通过 Webhook）
// Mailgun 会将收到的邮件 POST 到你的服务器
```

**优点**：
- ✅ 有免费额度
- ✅ 官方支持
- ✅ 稳定可靠
- ✅ 完整的 API

**缺点**：
- ❌ 需要域名
- ❌ 需要配置 DNS
- ❌ 超过免费额度需付费

---

### 方案 3: 使用 Gmail API (免费) ⭐

**特点**：
- ✅ 完全免费
- ✅ Google 官方
- ✅ 稳定可靠
- ✅ 能接收真实邮件

**实现步骤**：

#### 1. 创建 Google Cloud 项目
```
https://console.cloud.google.com/
```

#### 2. 启用 Gmail API
```
在 API 库中搜索 Gmail API
点击启用
```

#### 3. 创建 OAuth 凭据
```
创建 OAuth 2.0 客户端 ID
下载凭据 JSON 文件
```

#### 4. 使用 API
```javascript
const {google} = require('googleapis');
const gmail = google.gmail('v1');

// 获取邮件列表
gmail.users.messages.list({
  userId: 'me',
  q: 'is:unread'
});

// 读取邮件
gmail.users.messages.get({
  userId: 'me',
  id: messageId
});
```

**优点**：
- ✅ 完全免费
- ✅ Google 官方
- ✅ 稳定可靠
- ✅ 能接收真实邮件

**缺点**：
- ❌ 需要 OAuth 认证
- ❌ 配置复杂
- ❌ 需要 Google 账号

---

### 方案 4: 使用 Cloudflare Email Routing (免费) ⭐⭐⭐ 强烈推荐

**特点**：
- ✅ 完全免费
- ✅ 无限邮件
- ✅ Cloudflare 官方
- ✅ 配置简单

**实现步骤**：

#### 1. 注册 Cloudflare
```
https://www.cloudflare.com/
```

#### 2. 添加域名
```
1. 添加你的域名到 Cloudflare
2. 修改域名 DNS 服务器
```

#### 3. 启用 Email Routing
```
1. 进入域名管理
2. 点击 Email Routing
3. 添加路由规则
```

#### 4. 配置转发
```
将 *@your-domain.com 转发到你的真实邮箱
```

#### 5. 通过 API 访问
```javascript
// Cloudflare 提供 Workers 可以处理邮件
// 可以将邮件存储到 KV 或 R2
```

**优点**：
- ✅ 完全免费
- ✅ 无限邮件
- ✅ 配置简单
- ✅ Cloudflare 稳定

**缺点**：
- ❌ 需要域名
- ❌ 只能转发，不能直接接收

---

### 方案 5: 自建完整邮件服务器 (免费但复杂)

**技术栈**：
- Postfix (SMTP 服务器)
- Dovecot (IMAP/POP3 服务器)
- Roundcube (Web 邮件客户端)

**实现步骤**：

#### 1. 准备服务器
```
需要一台 VPS 服务器
推荐: Vultr, DigitalOcean, AWS
价格: $5-10/月
```

#### 2. 安装邮件服务器
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postfix dovecot-core dovecot-imapd

# 配置 Postfix
sudo dpkg-reconfigure postfix

# 配置 Dovecot
sudo nano /etc/dovecot/dovecot.conf
```

#### 3. 配置 DNS
```
MX 记录: mail.your-domain.com
A 记录: mail.your-domain.com -> 服务器 IP
SPF 记录: v=spf1 mx ~all
DKIM 记录: 配置 DKIM 密钥
```

#### 4. 安装 Web 界面
```bash
sudo apt install roundcube
```

**优点**：
- ✅ 完全控制
- ✅ 无限邮件
- ✅ 可以自定义

**缺点**：
- ❌ 需要服务器（付费）
- ❌ 配置复杂
- ❌ 需要维护
- ❌ 容易被标记为垃圾邮件

---

## 📊 方案对比

| 方案 | 费用 | 难度 | 可靠性 | 推荐度 |
|------|------|------|--------|--------|
| **MailCatcher** | 免费 | ⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Mailgun** | 免费额度 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Gmail API** | 免费 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Cloudflare** | 免费 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **自建服务器** | $5-10/月 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

---

## 💡 我的推荐

### 场景 1: 本地开发测试

**推荐**: MailCatcher
```
docker run -d -p 1080:1080 -p 1025:1025 schickling/mailcatcher
```

**理由**：
- 完全免费
- 配置简单
- 适合开发测试

---

### 场景 2: 生产环境（有域名）

**推荐**: Cloudflare Email Routing + Mailgun

**配置**：
```
1. Cloudflare Email Routing 接收邮件
2. 转发到 Mailgun 处理
3. 通过 Mailgun API 获取邮件
```

**理由**：
- 完全免费（Mailgun 免费额度足够）
- 稳定可靠
- 配置简单

---

### 场景 3: 生产环境（无域名）

**推荐**: Gmail API

**理由**：
- 完全免费
- Google 官方
- 稳定可靠

---

## 🚀 立即实现：MailCatcher 方案

### 步骤 1: 安装 Docker

**Windows**:
```
下载: https://www.docker.com/products/docker-desktop/
安装并启动
```

### 步骤 2: 运行 MailCatcher

```bash
docker run -d -p 1080:1080 -p 1025:1025 schickling/mailcatcher
```

### 步骤 3: 集成到我们的系统

我可以帮你实现：
```javascript
// 添加 MailCatcher 服务
class MailCatcherService {
  async getMessages() {
    const response = await axios.get('http://localhost:1080/messages');
    return response.data;
  }
  
  async getMessage(id) {
    const response = await axios.get(`http://localhost:1080/messages/${id}.json`);
    return response.data;
  }
}
```

### 步骤 4: 使用

```
1. 启动 MailCatcher
2. 配置 Windsurf 发送邮件到 localhost:1025
3. 在 http://localhost:1080 查看邮件
4. 或通过我们的系统 API 获取
```

---

## 💰 付费方案对比

### Mailgun
```
免费版: 5000 封/月
Foundation: $35/月 (50,000 封)
Growth: $80/月 (100,000 封)
```

### SendGrid
```
免费版: 100 封/天
Essentials: $19.95/月 (50,000 封)
Pro: $89.95/月 (100,000 封)
```

### Amazon SES
```
$0.10 / 1000 封邮件
非常便宜
但需要 AWS 账号
```

### Postmark
```
免费版: 100 封/月
$10/月: 10,000 封
$50/月: 50,000 封
```

---

## 🎯 最终建议

### 立即可用（免费）

**方案**: MailCatcher
```
1. 安装 Docker
2. 运行 MailCatcher
3. 我帮你集成到系统
4. 开始使用
```

### 长期方案（免费）

**方案**: Cloudflare Email Routing
```
1. 购买域名（约 $10/年）
2. 添加到 Cloudflare
3. 配置 Email Routing
4. 完全免费使用
```

### 付费方案（推荐）

**方案**: Mailgun
```
免费额度: 5000 封/月
足够个人使用
超出后 $35/月
```

---

## 📝 下一步

**你想选择哪个方案？**

1. **MailCatcher** - 我帮你立即实现（需要安装 Docker）
2. **Cloudflare** - 我提供详细配置教程（需要域名）
3. **Gmail API** - 我帮你实现（需要 Google 账号）
4. **Mailgun** - 我帮你实现（有免费额度）

**告诉我你的选择，我立即帮你实现！**

---

## 🎊 总结

**免费方案**：
- ✅ MailCatcher（本地）
- ✅ Cloudflare Email Routing（需要域名）
- ✅ Gmail API（需要 Google 账号）

**付费方案**：
- 💰 Mailgun: $35/月（有免费额度）
- 💰 SendGrid: $19.95/月
- 💰 Amazon SES: $0.10/1000封

**我的推荐**：
1. 开发测试：MailCatcher
2. 生产环境：Cloudflare + Mailgun
3. 简单快速：Gmail API

**现在就告诉我你的选择，我立即帮你实现！** 🚀
