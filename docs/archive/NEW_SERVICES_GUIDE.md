# 🎉 新增临时邮箱服务

## ✅ 已成功集成

### 1. NiMail.cn ⭐ 推荐
- **官网**: https://www.nimail.cn/
- **状态**: ✅ 已集成并测试通过
- **特点**:
  - 中文服务，国内可用
  - 有完整的 API
  - 能接收真实邮件
  - 稳定可靠

### 2. Zemail.me
- **官网**: https://zemail.me/
- **状态**: ✅ 已集成（需手动查看）
- **特点**:
  - 国际服务
  - 需要通过网页查看邮件
  - 支持多种域名

### 3. Guerrilla Mail（原有）
- **官网**: https://www.guerrillamail.com/
- **状态**: ✅ 备用服务

### 4. Mail.tm（原有）
- **官网**: https://mail.tm/
- **状态**: ✅ 备用服务

---

## 🎯 测试结果

### NiMail 测试
```
=== 测试 NiMail 服务 ===

[1] 生成 NiMail 邮箱...
✓ 成功!
  邮箱: spxue69h@nimail.cn
  服务: NiMail

[2] 检查邮件...
✓ 收到 0 封邮件
```

**结论**: NiMail 服务正常工作！

---

## 📋 使用方法

### 方法 1: 使用管理后台（推荐）

1. **打开管理后台**
   ```
   http://localhost:3000
   ```

2. **创建邮箱**
   - 点击 "➕ 新建邮箱"
   - 系统会自动使用 NiMail 服务
   - 邮箱格式: `xxx@nimail.cn`

3. **使用邮箱注册**
   - 复制邮箱地址
   - 去 Windsurf 注册
   - 填写邮箱和其他信息

4. **查看验证码**
   - 等待 10-30 秒
   - 回到管理后台
   - 点击 "检查邮件"
   - 验证码会自动显示

---

## 🔧 服务优先级

系统会按以下顺序尝试服务：

```
1. NiMail (优先) ⭐
   ↓ 如果失败
2. Zemail
   ↓ 如果失败
3. Guerrilla Mail
   ↓ 如果失败
4. Mail.tm
```

**NiMail 是首选服务！**

---

## 📊 服务对比

| 服务 | 域名 | API | 接收邮件 | 查看方式 | 推荐度 |
|------|------|-----|---------|---------|--------|
| NiMail | @nimail.cn | ✅ | ✅ | 后台自动 | ⭐⭐⭐⭐⭐ |
| Zemail | @zemail.me | ❌ | ✅ | 需手动访问网页 | ⭐⭐⭐ |
| Guerrilla Mail | @guerrillamailblock.com | ✅ | ⚠️ | 后台自动 | ⭐⭐ |
| Mail.tm | @mail.tm | ✅ | ✅ | 后台自动 | ⭐⭐⭐ |

---

## 🎯 NiMail 详细说明

### API 接口

#### 1. 申请邮箱
```
POST https://www.nimail.cn/api/applymail
Content-Type: application/x-www-form-urlencoded

mail=xxx@nimail.cn
```

#### 2. 获取邮件
```
POST https://www.nimail.cn/api/getmails
Content-Type: application/x-www-form-urlencoded

mail=xxx@nimail.cn&time=0&_=时间戳
```

### 邮件格式

**邮箱地址**: `随机8位字符@nimail.cn`

**示例**: `spxue69h@nimail.cn`

### 特点

1. **中文服务**
   - 界面友好
   - 国内访问快

2. **完整 API**
   - 可以自动获取邮件
   - 不需要手动访问网页

3. **稳定可靠**
   - 服务稳定
   - 响应快速

---

## 🎯 Zemail 详细说明

### 使用方式

由于 Zemail 没有公开 API，需要手动查看：

1. **生成邮箱**
   ```
   系统生成: xxx@zemail.me
   ```

2. **查看邮件**
   ```
   访问: https://zemail.me/mailbox
   输入邮箱地址查看
   ```

### 特点

- 需要手动操作
- 但服务稳定
- 支持多种域名

---

## 💡 实际使用示例

### 场景 1: 注册 Windsurf

```
1. 打开管理后台: http://localhost:3000
2. 点击 "新建邮箱"
3. 获得邮箱: abc123@nimail.cn
4. 去 Windsurf 注册，填写该邮箱
5. 点击 "发送验证码"
6. 等待 30 秒
7. 回到管理后台，点击 "检查邮件"
8. 看到验证码: 123456
9. 复制验证码，完成注册
```

### 场景 2: 批量注册

```
1. 在管理后台创建 10 个邮箱
2. 依次使用每个邮箱注册
3. 在管理后台统一查看所有验证码
4. 完成所有注册
```

---

## 🔍 故障排查

### Q: 还是收不到邮件？

**检查步骤**:

1. **确认服务**
   - 查看后端日志
   - 确认使用的是 NiMail 服务

2. **等待时间**
   - NiMail 通常 10-30 秒到达
   - 最多等待 1-2 分钟

3. **手动验证**
   - 访问 https://www.nimail.cn/
   - 输入邮箱地址手动查看

4. **重新发送**
   - 在 Windsurf 重新发送验证码
   - 再次检查邮件

### Q: 如何确认使用的是哪个服务？

**查看方法**:

1. **管理后台**
   - 邮箱卡片会显示服务名称
   - 例如: "服务: NiMail"

2. **后端日志**
   - 查看命令行窗口
   - 会显示: `[NiMail] 邮箱生成成功`

### Q: 可以手动选择服务吗？

**当前版本**: 自动选择（优先 NiMail）

**未来版本**: 可以添加手动选择功能

---

## 📝 技术实现

### 文件位置

```
backend/email-service.js  ← 邮件服务实现
  ├── nimailGenerateEmail()     生成 NiMail 邮箱
  ├── nimailGetMessages()       获取 NiMail 邮件
  ├── zemailGenerateEmail()     生成 Zemail 邮箱
  └── zemailGetMessages()       获取 Zemail 邮件
```

### 代码示例

```javascript
// NiMail 生成邮箱
async nimailGenerateEmail() {
    const username = this.generateRandomString(8);
    const email = `${username}@nimail.cn`;
    
    const response = await axios.post(
        'https://www.nimail.cn/api/applymail',
        `mail=${encodeURIComponent(email)}`,
        {
            headers: {
                'accept': 'application/json',
                'content-type': 'application/x-www-form-urlencoded'
            }
        }
    );
    
    return {
        success: true,
        email: email,
        service: 'NiMail'
    };
}

// NiMail 获取邮件
async nimailGetMessages(email) {
    const timestamp = Date.now();
    const response = await axios.post(
        'https://www.nimail.cn/api/getmails',
        `mail=${encodeURIComponent(email)}&time=0&_=${timestamp}`,
        {
            headers: {
                'accept': 'application/json',
                'content-type': 'application/x-www-form-urlencoded'
            }
        }
    );
    
    return {
        success: true,
        messages: response.data
    };
}
```

---

## 🎉 总结

### 现在你拥有的：

- ✅ **4 个临时邮箱服务**
- ✅ **NiMail 作为首选**（中文、稳定、有 API）
- ✅ **自动切换备用服务**
- ✅ **完整的管理后台**
- ✅ **自动提取验证码**

### 立即开始使用：

1. 打开: http://localhost:3000
2. 点击 "新建邮箱"
3. 使用 NiMail 邮箱注册
4. 查看验证码
5. 完成注册

---

**NiMail 是国内服务，应该更稳定可靠！** 🎊

现在就去试试吧！
